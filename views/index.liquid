{% render 'partials/head.liquid' %}

<div class="card-container">
    <section class="card">

        <article class="front">

            <div class="circle">
                <img class="avatar" src="{{person.avatar}}" alt="profiel foto">
                <p class="spin-text first-spin">{{ custom.study }}</p>
                <p class="spin-text second-spin">{{ custom.study }}</p>
                <p class="spin-text third-spin">{{ custom.study }}</p>
                <p class="spin-text fourth-spin">{{ custom.study }}</p>
            </div>

            <div class="front-info"> <!-- container voor flex -->
                <h1>{{ person.name }}</h1>
                <p>{{ custom.about}}</p>
            </div>

            <p>flip voor meer &rarr;</p>

        </article>

        <article class="back">
            <h2>Over mij</h2>
            <ul class="button-list">
                <li><button class="theme-select" data-section="hobbys"><span>📺</span>Hobby's</button></li>
                <li><button class="theme-select" data-section="werkervaring"><span>💼</span>Werkervaring</button></li>
                <li><button class="theme-select" data-section="kennis"><span>💻</span>Kennis</button></li>
            </ul>

            <div class="content">
                <div id="hobbys" class="section-content">
                    <p>Mijn hobby's zijn {{ custom.hobby }}</p>
                    <button class="back-button">Terug</button>
                </div>
                <div id="werkervaring" class="section-content">
                    <p>{{ custom.werkervaring  }}</p>
                    <button class="back-button">Terug</button>
                </div>

                <div id="kennis" class="section-content">
                    <p>Mijn kennisgebieden zijn {{ custom.kennis }}</p>
                    <button class="back-button">Terug</button>
                </div>
            </div>
        </article>

    </section>
</div>

{% render 'partials/foot.liquid' %}